(강의 내용 중..)
- System 클래스에서 제공하는...
- .arraycopy / .in / .쓰고 쓰는..메소드?
- (word내용중)System.arraycopy 부분 설명에 복사가 시작되는 인덱스 라고 하는데 인덱스..
- 리턴이란?
  https://m.blog.naver.com/PostView.naver?isHttpsRedirect=true&blogId=light0a&logNo=140066907569
- .length(강의 첫부분에나옴)
- 메소드

-(word) 5.향상된 for문에서 for(int score : scores) {~} 이런거 처음 봤음
  	//1. 향상된 for문 (배열을 이용하여 초기화 된 변수 없이 for 사용 가능)
		int[] scores = {100, 90, 80};
		int sum = 0;
		//scores 배열의 요소 개수만큼 반복
		//scores 배열의 요소 하나씩 꺼내서 score 변수에 담아서 사용
		for(int score : scores) {
			sum += score;
		}		
		System.out.println("점수의 총합은 : " + sum);
-열거타입(Enum)은 Enum 따로 만들고 클래스에서 사용하는듯..
  Enum에서 Calendar cal = Calendar.getInstance(); 이게 나왔는데.. Calendar 변수도 있나..
  변수 타입 찾아볼것
-(word)8. 객체와 클래스 관계 - void(변수)
-변수, 클래스, 메소드 명명 규칙
-void형 메소드


